# Copy to .env and adjust if your backend runs somewhere other than localhost:8000
#   cp .env.example .env
#
# Not needed for production — once you `npm run build`, the build is served
# by Laravel itself (same origin), so this only matters while running the
# frontend as its own `npm run dev` process during local development.
VITE_API_BASE_URL=http://localhost:8000
